apiVersion: v1
kind: Service
metadata:
  name: go-app-service
spec:
  selector:
    app: go-app
  ports:
    - protocol: TCP
      port: 80        # Service port for ingress
      targetPort: 8080  # Container port 
  type: LoadBalancer
